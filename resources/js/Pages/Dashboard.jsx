import { Link } from '@inertiajs/react';
import StoreLayout from '@/Layouts/StoreLayout';
const money = n => (n / 100).toLocaleString('en-IE', { style: 'currency', currency: 'EUR' });
export default function Dashboard({ orders = [] }) { return <StoreLayout title="Your account"><main className="mx-auto max-w-5xl px-5 py-14 lg:px-8"><p className="text-xs font-bold uppercase tracking-[.25em] text-lime-600">Your space</p><h1 className="mt-3 text-4xl font-black">Account</h1><div className="mt-10 rounded-2xl bg-slate-950 p-7 text-white"><p className="text-sm text-slate-400">Welcome back</p><h2 className="mt-2 text-2xl font-black">Your movement, your way.</h2><Link href={route('store.catalog')} className="mt-6 inline-block rounded-full bg-lime-400 px-5 py-2.5 text-sm font-bold text-slate-950">Shop new gear ↗</Link></div><section className="mt-12"><h2 className="text-2xl font-black">Order history</h2>{orders.length === 0 ? <div className="mt-5 rounded-2xl bg-white p-8 text-slate-500">No orders yet. Your next adventure starts here.</div> : <div className="mt-5 space-y-3">{orders.map(order => <div key={order.id} className="flex flex-wrap items-center justify-between gap-4 rounded-2xl bg-white p-5"><div><p className="font-bold">{order.number}</p><p className="mt-1 text-sm text-slate-500">{order.items.length} items · {new Date(order.created_at).toLocaleDateString()}</p></div><div className="flex items-center gap-5"><span className="rounded-full bg-lime-100 px-3 py-1 text-xs font-bold text-lime-800">{order.status.replace('_', ' ')}</span><span className="font-bold">{money(order.total_cents)}</span></div></div>)}</div>}</section></main></StoreLayout>; }
