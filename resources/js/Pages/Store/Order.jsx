import { Link } from '@inertiajs/react'; import StoreLayout from '@/Layouts/StoreLayout';
const money = n => (n / 100).toLocaleString('en-IE', { style: 'currency', currency: 'EUR' });
export default function Order({ order }) { return <StoreLayout title={`Order ${order.number}`}><main className="mx-auto max-w-3xl px-5 py-20 text-center lg:px-8"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-lime-400 text-3xl">✓</div><p className="mt-7 text-xs font-bold uppercase tracking-[.25em] text-lime-600">Order confirmed</p><h1 className="mt-3 text-4xl font-black">Thanks for moving with us.</h1><p className="mt-4 text-slate-500">Your order <strong>{order.number}</strong> is being prepared. We sent confirmation to {order.email}.</p><div className="mx-auto mt-10 max-w-lg rounded-2xl bg-white p-6 text-left"><div className="space-y-3">{order.items.map(i => <div key={i.id} className="flex justify-between text-sm"><span>{i.name} × {i.quantity}</span><span className="font-bold">{money(i.line_total_cents)}</span></div>)}</div><div className="mt-5 border-t pt-5 flex justify-between font-black"><span>Total</span><span>{money(order.total_cents)}</span></div></div><Link href={route('store.catalog')} className="mt-8 inline-block rounded-full bg-slate-950 px-7 py-3 font-bold text-white">Continue shopping ↗</Link></main></StoreLayout>; }
