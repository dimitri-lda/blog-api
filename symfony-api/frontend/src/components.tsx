import { useState } from 'react';
import { Link,NavLink,Outlet,Navigate,useNavigate } from 'react-router-dom';
import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query';
import { api } from './api';
import { useAuth } from './auth';
import type { Cart,Product } from './types';
import { money } from './types';

export function Logo(){return <Link to="/" className="text-2xl font-black tracking-tight"><span className="text-blue-500">dao</span>Sport</Link>}
export function ProductCard({product}:{product:Product}){const client=useQueryClient();const add=useMutation({mutationFn:()=>api.post('/cart/items',{variant_id:product.variants[0]?.id,quantity:1}),onSuccess:()=>client.invalidateQueries({queryKey:['cart']})});return <article className="group"><Link to={`/shop/${product.slug}`} className="relative block overflow-hidden rounded-2xl bg-white"><img src={product.image_url??''} alt={product.name} className="aspect-square w-full object-cover transition duration-500 group-hover:scale-105"/><span className="absolute left-3 top-3 rounded-full bg-white/90 px-3 py-1 text-[10px] font-bold uppercase tracking-wider">{product.featured?'Featured':product.category.name}</span></Link><button onClick={()=>add.mutate()} aria-label={`Add ${product.name} to bag`} className="relative -mt-13 ml-auto mr-3 flex h-10 w-10 translate-y-2 items-center justify-center rounded-full bg-slate-950 text-lg text-white opacity-0 transition group-hover:translate-y-0 group-hover:opacity-100">+</button><div className="pt-4"><p className="text-xs font-semibold uppercase tracking-wider text-slate-400">{product.brand}</p><Link to={`/shop/${product.slug}`} className="mt-1 block font-bold hover:text-blue-600">{product.name}</Link><div className="mt-2 flex items-center gap-2"><span className="font-bold">{money(product.price_cents)}</span>{product.compare_at_price_cents&&<del className="text-sm text-slate-400">{money(product.compare_at_price_cents)}</del>}</div></div></article>}
export function StoreLayout(){const{user,logout}=useAuth();const{data}=useQuery({queryKey:['cart'],queryFn:()=>api.get<{data:Cart}>('/cart')});const[open,setOpen]=useState(false);const navigate=useNavigate();const search=(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();const q=new FormData(event.currentTarget).get('q');navigate(q?`/shop?q=${encodeURIComponent(String(q))}`:'/shop');};return <div className="min-h-screen bg-[#f7f8fa] text-slate-900"><div className="bg-slate-950 px-4 py-2 text-center text-xs font-medium tracking-wide text-white">Free shipping on orders over €50 · Easy 30-day returns</div><header className="sticky top-0 z-40 border-b border-slate-200/80 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center gap-5 px-5 py-4 lg:px-8"><button onClick={()=>setOpen(!open)} className="rounded-lg p-2 lg:hidden" aria-label="Open menu">☰</button><Logo/><nav className={`${open?'absolute left-0 right-0 top-full flex':'hidden'} flex-col gap-4 border-b bg-white p-5 text-sm font-semibold lg:static lg:flex lg:flex-row lg:border-0 lg:bg-transparent lg:p-0`}><Link to="/shop" onClick={()=>setOpen(false)}>Shop all</Link><Link to="/shop?category=running" onClick={()=>setOpen(false)}>Running</Link><Link to="/shop?category=fitness" onClick={()=>setOpen(false)}>Fitness</Link><Link to="/shop?category=tennis" onClick={()=>setOpen(false)}>Racket sports</Link><Link to="/shop?category=outdoor" onClick={()=>setOpen(false)}>Outdoor</Link></nav><form onSubmit={search} className="ml-auto hidden max-w-xs flex-1 md:flex"><input name="q" placeholder="Search products, brands..." className="w-full rounded-full border border-slate-200 bg-slate-50 px-5 py-2.5 text-sm outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-200"/></form><div className="flex items-center gap-2 text-sm"><div className="group relative hidden sm:block"><Link to={user?'/dashboard':'/login'} className="flex items-center gap-2 rounded-full px-3 py-2 transition hover:bg-slate-100" aria-label="Account menu"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" className="h-5 w-5"><circle cx="12" cy="8" r="3.25"/><path strokeLinecap="round" d="M5 20c.8-3.15 3.2-5 7-5s6.2 1.85 7 5"/></svg><span className="hidden xl:inline">Account</span></Link><div className="invisible absolute right-0 top-full z-50 w-56 pt-2 opacity-0 transition duration-150 group-hover:visible group-hover:opacity-100 group-focus-within:visible group-focus-within:opacity-100"><div className="rounded-2xl border border-slate-200 bg-white p-2 shadow-xl shadow-slate-900/10">{user?<><p className="px-3 py-2 text-xs font-medium text-slate-500">Signed in as {user.name}</p><Link to="/dashboard" className="block rounded-xl px-3 py-2 text-sm font-semibold hover:bg-slate-100">My account</Link><Link to="/profile" className="block rounded-xl px-3 py-2 text-sm font-semibold hover:bg-slate-100">Profile settings</Link><button onClick={()=>void logout()} className="mt-1 block w-full rounded-xl px-3 py-2 text-left text-sm font-semibold hover:bg-slate-100">Log out</button></>:<><p className="px-3 py-2 text-xs font-medium text-slate-500">Have an account?</p><Link to="/login" className="block rounded-xl px-3 py-2 text-sm font-semibold hover:bg-slate-100">Log in</Link><Link to="/register" className="mt-1 block rounded-xl px-3 py-2 text-sm font-semibold hover:bg-slate-100">Create account</Link></>}</div></div></div><div className="group relative"><Link to="/cart" className="flex items-center gap-2 rounded-full bg-slate-950 px-3 py-2.5 font-semibold text-white transition hover:bg-slate-800" aria-label="Shopping bag menu"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" className="h-5 w-5"><path strokeLinecap="round" strokeLinejoin="round" d="M5 8h14l-1 12H6L5 8Z"/><path strokeLinecap="round" d="M9 10V6a3 3 0 1 1 6 0v4"/></svg><span className="hidden sm:inline">Bag</span><span className="text-blue-300">{data?.data.count??0}</span></Link><div className="invisible absolute right-0 top-full z-50 w-56 pt-2 opacity-0 transition duration-150 group-hover:visible group-hover:opacity-100 group-focus-within:visible group-focus-within:opacity-100"><div className="rounded-2xl border border-slate-200 bg-white p-2 text-slate-900 shadow-xl shadow-slate-900/10"><p className="px-3 py-2 text-xs font-medium text-slate-500">Ready to check out?</p><Link to="/cart" className="block rounded-xl px-3 py-2 text-sm font-semibold hover:bg-slate-100">View bag</Link><Link to="/checkout" className="mt-1 block rounded-xl px-3 py-2 text-sm font-semibold hover:bg-slate-100">Checkout</Link></div></div></div></div></div></header><Outlet/><footer className="mt-20 bg-slate-950 text-white"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-14 sm:grid-cols-2 lg:grid-cols-4 lg:px-8"><div><div className="text-2xl font-black"><span className="text-blue-400">dao</span>Sport</div><p className="mt-4 max-w-xs text-sm leading-6 text-slate-400">Equipment for every way you move. Curated gear, honest advice and fast delivery.</p></div><div><h3 className="font-bold">Shop</h3><div className="mt-4 space-y-3 text-sm text-slate-400"><Link to="/shop" className="block">All products</Link><Link to="/shop?category=running" className="block">Running</Link><Link to="/shop?category=fitness" className="block">Fitness</Link></div></div><div><h3 className="font-bold">Help</h3><div className="mt-4 space-y-3 text-sm text-slate-400"><p>Delivery & returns</p><p>Contact us</p><p>Size guide</p></div></div><div><h3 className="font-bold">Stay in the loop</h3><p className="mt-4 text-sm text-slate-400">New drops and training tips, once a week.</p><div className="mt-4 flex"><input className="w-full rounded-l-lg border-0 bg-slate-800 px-3 text-sm" placeholder="Email address"/><button className="rounded-r-lg bg-blue-400 px-4 font-bold text-slate-950">→</button></div></div></div><div className="border-t border-slate-800 py-5 text-center text-xs text-slate-500">© 2026 daoSport · EUR · Europe</div></footer></div>}
export function GuestLayout(){return <div className="flex min-h-screen flex-col bg-[#f7f8fa]"><header className="border-b bg-white px-6 py-5"><Logo/></header><main className="flex flex-1 items-center justify-center px-5 py-12"><div className="w-full max-w-md rounded-3xl bg-white px-7 py-8 shadow-sm sm:px-10"><Outlet/></div></main><footer className="py-6 text-center text-xs text-slate-400">© 2026 daoSport · Europe</footer></div>}
export function AdminLayout(){const{user,logout}=useAuth();return <div className="min-h-screen bg-slate-100 lg:grid lg:grid-cols-[260px_1fr]"><aside className="bg-slate-950 p-6 text-white lg:min-h-screen"><div className="text-xl font-black"><span className="text-blue-400">dao</span>Sport <span className="text-xs uppercase tracking-widest text-slate-400">admin</span></div><nav className="mt-10"><NavLink to="/admin/orders" className={({isActive})=>`block rounded-xl px-4 py-3 text-sm font-semibold ${isActive?'bg-blue-400 text-slate-950':'text-slate-300'}`}>Orders</NavLink></nav><div className="mt-10 border-t border-slate-800 pt-6"><p className="font-semibold">{user?.name}</p><p className="text-xs text-slate-400">{user?.email}</p><button onClick={()=>void logout()} className="mt-4 text-sm text-slate-300">Log out</button></div></aside><main className="p-5 md:p-10"><Outlet/></main></div>}
export function Protected({admin=false}:{admin?:boolean}){const{user,ready}=useAuth();if(!ready)return <div className="grid min-h-screen place-items-center font-bold">Loading…</div>;if(!user)return <Navigate to="/login" replace/>;if(admin&&!user.roles.some(role=>['ROLE_ADMIN','ROLE_SUPERADMIN'].includes(role)))return <Navigate to="/" replace/>;return <Outlet/>}
export function ErrorText({message}:{message?:string}){return message?<p className="mt-1 text-xs font-semibold text-red-600">{message}</p>:null}
